// import { IUser } from "@/models/models"

// export const pushLogin: (activeUser: IUser, )=>void = (activeUser, router) => {
//     if (Object.keys(activeUser).length === 0) {
//         router.push({
//             pathname: '/login'
//         })
//     }
// }
